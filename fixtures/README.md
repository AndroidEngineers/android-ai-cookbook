# Fixtures

Add synthetic, distributable inputs and expected outputs alongside recipe implementations. Record what each fixture tests. Never include credentials, real user records, or proprietary documents.
